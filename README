libakdtree
==========

This is a simple KD-Tree library, originally copied from
<https://github.com/AReallyGoodName/OfflineReverseGeocode/> (MIT-licensed).

Compiling
=========

Just `make` ought to do.

Unit tests
==========

`make -C tests test`

Installing
==========

Install `libakdtree.so.*` and `akdtree.h` wherever such files are installed on
your operating system.
